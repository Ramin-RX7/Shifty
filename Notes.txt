Classes:
    "final" decorator same as python but strict
    "dataclass" decorator



How to get list of attributes/methods of a class/object


For primitive types like "int", how to implement math operations?
We can have a list of types (basically all these primitive types) where their behavior totaly \
depends on the base language and only their proto-types are defined in Shifty



Set the function prototype constant. This means if it is inherited it can not overwrite the prototype.
    - final_prototype decorator: the argument name/count and data type can not be changed.




waiting for doc:
- call stack
- How does Task, InterruptibleTask work




Not implemented errors:
- OS
- ZeroDevision
- Memory
- StopIteration
- Import
- System





STD Lib:

Types:
    FreeList  (Same as lists without restriction of having elements with same type)
   #AssignedValueEnums (Same as enums but able to set values to their attributes)
